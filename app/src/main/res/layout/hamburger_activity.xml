<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.arivald.player.presentation.hamburger.HamburgerActivity">

    <data>

        <import type="com.example.arivald.player.R" />

        <variable
            name="viewModel"
            type="com.example.arivald.player.presentation.hamburger.HamburgerViewModel" />

        <variable
            name="presenter"
            type="com.example.arivald.player.presentation.hamburger.HamburgerPresenter" />
    </data>

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/drawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="false"
        app:binding="@{viewModel.drawer}"
        app:closeDrawerContentDesc="@{R.string.navigation_drawer_close}"
        app:openDrawerContentDesc="@{R.string.navigation_drawer_open}"
        app:toolbar="@{@id/toolbar}"
        tools:openDrawer="start">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                app:popupTheme="@style/AppTheme.PopupOverlay" />

            <FrameLayout
                android:id="@+id/content"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin" />

        </LinearLayout>

        <android.support.design.widget.NavigationView

            android:id="@+id/navigation_view"

            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:layout_marginTop="100dp"
            android:background="@drawable/nav_background"
            android:fitsSystemWindows="true"
            app:headerLayout="@layout/hamburger_nav_header"
            app:itemIconTint="@color/white"
            app:itemTextColor="?android:textColorPrimary"
            app:menu="@menu/hamburger_activity_drawer" />

    </android.support.v4.widget.DrawerLayout>

</layout>