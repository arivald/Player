<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/general_background</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PlayerTheme" parent="AppTheme" />

    <style name="AppTheme.AppBarOverlay" parent="PlayerTheme" />

    <style name="AppTheme.PopupOverlay" parent="PlayerTheme" />


    <style name="nav_header_mc_button" >
        <item name="android:layout_width">@dimen/nav_header_mc_button_dimension</item>
        <item name="android:layout_height">@dimen/nav_header_mc_button_dimension</item>
        <item name="android:layout_marginLeft">@dimen/nav_header_mc_button_margin</item>
        <item name="android:layout_marginRight">@dimen/nav_header_mc_button_margin</item>
        <item name="android:background">?selectableItemBackgroundBorderless</item>
        <item name="android:layout_alignParentBottom">true</item>
    </style>

</resources>
